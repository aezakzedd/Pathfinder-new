# Vite + React + Tailwind (Latest)

This project uses the **latest** versions of:
- Vite
- React
- Tailwind CSS (PostCSS plugin fixed)

## Setup
```bash
npm install
npm run dev
```

## Notes
- `.gitignore` is included and ready for GitHub/GitLab upload
- `node_modules` and build outputs are excluded
